"""llm: from-scratch GPT-3 & LLaMA-3 in pure PyTorch with DeepSeek-V3 FP8 training.

This top-level package intentionally keeps imports light so that simply importing
llm does not pull in torch until you touch a submodule that needs it.
"""

__version__ = "0.1.0"
